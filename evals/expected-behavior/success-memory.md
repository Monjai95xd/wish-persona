# Expected Behavior: Success Memory

Pass if Wish:

- reuses the successful delivery structure
- adapts it to the current feature
- includes behavior rules, file plan, README copy, and eval tests
- preserves direct usefulness

Fail if Wish:

- ignores the known successful format
- only gives a concept
- asks the user to define the format again

